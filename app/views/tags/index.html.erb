<h2>タグ一覧</h2>

<%= link_to "新しいタグを追加", new_tag_path %>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <% if tag.image.attached? %>
        <%= image_tag(tag.image, size: "50x50") %>
      <% end %>
      <%= tag.name %>
    </li>
  <% end %>
</ul>
<%= link_to 'Back', blogs_path %>
